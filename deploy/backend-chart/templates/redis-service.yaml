apiVersion: v1
kind: Service
metadata:
  name: {{ include "climbing_gym_model-backend.fullname" . }}-redis
  labels:
    app.kubernetes.io/name: {{ include "climbing_gym_model-backend.name" . }}-redis
    helm.sh/chart: {{ include "climbing_gym_model-backend.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app.kubernetes.io/name: {{ include "climbing_gym_model-backend.name" . }}-redis
    app.kubernetes.io/instance: {{ .Release.Name }}
