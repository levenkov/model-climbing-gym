apiVersion: v1
kind: Secret
metadata:
  name: {{ include "climbing_gym_model-backend.fullname" . }}-rails-credentials
  labels:
    app.kubernetes.io/name: {{ include "climbing_gym_model-backend.name" . }}
    helm.sh/chart: {{ include "climbing_gym_model-backend.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
type: Opaque
data:
  production.yml.enc: {{ .Values.credentials.productionYmlEnc | b64enc }}
  production.key: {{ .Values.credentials.productionKey | b64enc }}
