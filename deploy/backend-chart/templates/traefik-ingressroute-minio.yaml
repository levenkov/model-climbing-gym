apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "climbing_gym_model-backend.fullname" . }}-minio
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "climbing_gym_model-backend.name" . }}-minio
    helm.sh/chart: {{ include "climbing_gym_model-backend.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  entryPoints:
    - web
  routes:
    - match: {{ if .Values.gateway.host }}Host(`{{ .Values.gateway.host }}`) && {{ end }}PathPrefix(`/minio`)
      kind: Rule
      middlewares:
        - name: {{ include "climbing_gym_model-backend.fullname" . }}-minio-stripprefix
      services:
        - name: {{ include "climbing_gym_model-backend.fullname" . }}-minio
          port: 9000
