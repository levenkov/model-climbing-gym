<nav class="bg-white shadow mb-6">
  <div class="max-w-5xl mx-auto px-8">
    <div class="flex items-center justify-between h-14">
      <%= link_to 'Climbing Gym Model', root_path, class: 'text-lg font-bold text-indigo-600 hover:text-indigo-800' %>

      <div class="flex items-center gap-6 text-sm">
        <% if current_user %>
          <span class="text-gray-600"><%= current_user.name %></span>
          <%= link_to 'Profile', users_me_path, class: 'text-gray-600 hover:text-gray-900' %>
          <%= link_to 'Log out', destroy_user_session_path, data: { turbo_method: :delete }, class: 'text-gray-600 hover:text-gray-900' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
